import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import HearthstoneTracker from './hearthstone-tracker.tsx';

ReactDOM.render(
  <React.StrictMode>
    <HearthstoneTracker />
  </React.StrictMode>,
  document.getElementById('root')
);